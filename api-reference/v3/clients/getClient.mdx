---
title: "Get Client"
description: "Retrieve a client user and parent organization"
openapi: "GET /clients/{clientId}"
---

<Note>
Returns `clientData` and `orgData` when available. Password hashes are never returned.
</Note>
