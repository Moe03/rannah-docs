---
title: "Get Agency"
description: "Get the agency for the authenticated workspace"
openapi: "GET /agency"
---

<Note>
Returns the whitelabel agency document for the workspace authenticated by `workspaceSecret`. `data` may be `null` if none exists.
</Note>
