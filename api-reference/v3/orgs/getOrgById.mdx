---
title: "Get Organization"
description: "Retrieve a single organization by ID"
openapi: "GET /orgs/{orgId}"
---

<Note>
Returns one organization owned by the authenticated workspace, including assigned agent IDs (`widgetIDs`).
</Note>
